use_default_settings: true

general:
  debug: false
  instance_name: QUIP Search

search:
  safe_search: 0
  formats:
    - html
    - json

server:
  secret_key: change-me-through-SEARXNG_SECRET
  limiter: false
  public_instance: false
  image_proxy: false
